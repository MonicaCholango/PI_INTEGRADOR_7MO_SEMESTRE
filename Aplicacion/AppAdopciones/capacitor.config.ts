import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ec.pae.adopciones',
  appName: 'AppAdopciones',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;